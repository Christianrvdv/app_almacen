{% extends 'base.html.twig' %}

{% block title %}Editar Venta{% endblock %}

{% block body %}
    <div class="container mt-5">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <a href="{{ path('app_venta_index') }}" class="btn btn-outline-secondary btn-sm">
                <i class="fas fa-arrow-left me-1"></i>Regresar al listado
            </a>
            <h1 class="fw-bold text-primary">Editar Venta #{{ venta.id }}</h1>
            <form method="post" action="{{ path('app_venta_delete', {'id': venta.id}) }}"
                  onsubmit="return confirm('¿Estás seguro de que deseas eliminar esta venta?');"
                  style="display: inline-block;">
                <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ venta.id) }}">
                <button type="submit" class="btn btn-danger btn-sm ms-2">
                    <i class="fas fa-trash me-1"></i>Eliminar Venta
                </button>
            </form>
        </div>

        <div class="card shadow-sm border-0 mb-4">
            <div class="card-body">
                {{ form_start(form) }}
                <div class="row">
                    <div class="col-md-6 mb-3">
                        <div class="form-group">
                            {{ form_row(form.fecha, {'attr': {'class': 'form-control'}}) }}
                        </div>
                        <div class="form-group">
                            {{ form_row(form.total, {'attr': {'class': 'form-control'}}) }}
                        </div>
                    </div>
                    <div class="col-md-6 mb-3">
                        <div class="form-group">
                            {{ form_row(form.tipo_veenta, {'attr': {'class': 'form-control'}}) }}
                        </div>
                        <div class="form-group">
                            {{ form_row(form.estado, {'attr': {'class': 'form-control'}}) }}
                        </div>
                    </div>
                </div>

                <div class="row mb-4">
                    <div class="col-12">
                        <div class="form-group mb-3">
                            {{ form_row(form.cliente, {'attr': {'class': 'form-control'}}) }}
                        </div>
                    </div>
                </div>

                <div class="mb-4">
                    <h5 class="mt-4 fw-bold text-primary">Detalles de la venta</h5>
                    <div id="detalle-ventas" data-prototype="{{ form_widget(form.detalleVentas.vars.prototype)|e('html_attr') }}">
                        {% for detalle in form.detalleVentas %}
                            <div class="card bg-light mb-3" style="border-radius: 8px; padding: 15px;">
                                <div class="row align-items-center">
                                    <div class="col-md-3 mb-3">
                                        <div class="form-group">
                                            {{ form_row(detalle.cantidad, {'attr': {'class': 'form-control'}}) }}
                                        </div>
                                    </div>
                                    <div class="col-md-3 mb-3">
                                        <div class="form-group">
                                            {{ form_row(detalle.precio_unitario, {'attr': {'class': 'form-control'}}) }}
                                        </div>
                                    </div>
                                    <div class="col-md-6 mb-3">
                                        <div class="form-group">
                                            {{ form_row(detalle.producto, {'attr': {'class': 'form-control'}}) }}
                                        </div>
                                    </div>

                                    <div class="col-md-6 mb-3">
                                        <div class="form-group">
                                            {{ form_row(detalle.precio_costo, {'attr': {'class': 'form-control'}}) }}
                                        </div>
                                    </div>
                                </div>
                                <button type="button" class="btn btn-sm btn-danger eliminar-detalle btn-block">
                                    Eliminar
                                </button>
                            </div>
                        {% endfor %}
                    </div>
                </div>

                <div class="d-flex justify-content-between align-items-center mb-4">
                    <button type="button" id="add-detalle" class="btn btn-outline-primary">
                        <i class="fas fa-plus me-1"></i>Agregar Detalle
                    </button>
                    <button type="submit" class="btn btn-primary">Actualizar venta</button>
                </div>
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}

{% block javascripts %}
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            const addButton = document.getElementById('add-detalle');
            const detalleVentas = document.getElementById('detalle-ventas');
            const prototype = detalleVentas.getAttribute('data-prototype');

            addButton.addEventListener('click', function() {
                const newForm = prototype.replace(/__name__/g, Date.now());
                const newRow = document.createElement('div');
                newRow.setAttribute('class', 'card bg-light mb-3');
                newRow.setAttribute('style', 'border-radius: 8px; padding: 15px;');
                newRow.innerHTML = newForm;

                const deleteButton = document.createElement('button');
                deleteButton.setAttribute('type', 'button');
                deleteButton.setAttribute('class', 'btn btn-sm btn-danger eliminar-detalle mt-2 btn-block');
                deleteButton.innerHTML = 'Eliminar';
                newRow.appendChild(deleteButton);

                detalleVentas.appendChild(newRow);
            });

            detalleVentas.addEventListener('click', function(e) {
                if (e.target.classList.contains('eliminar-detalle')) {
                    e.target.closest('.card').remove();
                }
            });
        });
    </script>
{% endblock %}
