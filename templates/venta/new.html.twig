{% extends 'base.html.twig' %}

{% block title %}Nueva Venta{% endblock %}

{% block body %}
    <div class="container mt-5">
        <div class="d-flex justify-content-between align-items-center mb-4">

            <a href="{{ path('app_venta_index') }}" class="btn btn-outline-secondary btn-sm">
                <i class="fas fa-arrow-left me-1"></i>Regresar al listado
            </a>
            <h1 class="fw-bold text-primary">Nueva Venta</h1>

            <a href="{{ path('app_detalle_venta_index') }}" class="btn btn-outline-primary btn-sm">
                <i class="fas fs-1 me-1"></i>Detalle venta
            </a>
        </div>

        <div class="card shadow-sm border-0">
            <div class="card-body">
                {{ include('venta/_form.html.twig', {'button_label': 'Guardar venta'}) }}
            </div>
        </div>
    </div>
{% endblock %}
