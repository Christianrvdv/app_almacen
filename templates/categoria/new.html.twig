{% extends 'base.html.twig' %}

{% block title %}Nueva Categoría{% endblock %}

{% block body %}
    <div class="container mt-5">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h1 class="fw-bold text-primary">Nueva Categoría</h1>
            <a href="{{ path('app_categoria_index') }}" class="btn btn-outline-secondary btn-sm">
                <i class="fas fa-arrow-left me-1"></i>Regresar al listado
            </a>
        </div>

        <div class="card shadow-sm border-0">
            <div class="card-body">
                {{ include('categoria/_form.html.twig', {'button_label': 'Guardar categoría'}) }}
            </div>
        </div>
    </div>
{% endblock %}
