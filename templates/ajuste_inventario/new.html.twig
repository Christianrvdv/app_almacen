{% extends 'base.html.twig' %}

{% block title %}Nuevo Ajuste de Inventario{% endblock %}

{% block body %}
    <div class="container mt-5">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h1 class="fw-bold text-primary">Nuevo Ajuste de Inventario</h1>
            <a href="{{ path('app_ajuste_inventario_index') }}" class="btn btn-outline-secondary btn-sm">
                <i class="fas fa-arrow-left me-1"></i>Regresar al listado
            </a>
        </div>

        <div class="card shadow-sm border-0">
            <div class="card-body">
                {{ include('ajuste_inventario/_form.html.twig', {'button_label': 'Guardar ajuste'}) }}
            </div>
        </div>
    </div>
{% endblock %}
