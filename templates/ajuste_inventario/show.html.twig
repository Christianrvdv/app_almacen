{% extends 'base.html.twig' %}

{% block title %}AjusteInventario{% endblock %}

{% block body %}
    <h1>AjusteInventario</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ajuste_inventario.id }}</td>
            </tr>
            <tr>
                <th>Tipo</th>
                <td>{{ ajuste_inventario.tipo }}</td>
            </tr>
            <tr>
                <th>Cantidad</th>
                <td>{{ ajuste_inventario.cantidad }}</td>
            </tr>
            <tr>
                <th>Fecha</th>
                <td>{{ ajuste_inventario.fecha ? ajuste_inventario.fecha|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Motivo</th>
                <td>{{ ajuste_inventario.motivo }}</td>
            </tr>
            <tr>
                <th>Usuario</th>
                <td>{{ ajuste_inventario.usuario }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_ajuste_inventario_index') }}">back to list</a>

    <a href="{{ path('app_ajuste_inventario_edit', {'id': ajuste_inventario.id}) }}">edit</a>

    {{ include('ajuste_inventario/_delete_form.html.twig') }}
{% endblock %}
