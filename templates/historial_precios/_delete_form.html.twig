<form method="post" action="{{ path('app_historial_precios_delete', {'id': historial_precio.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ historial_precio.id) }}">
    <button class="btn">Delete</button>
</form>
